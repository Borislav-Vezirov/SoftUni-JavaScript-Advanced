function generateReport() {
    let checkbox = Array.from(document.querySelectorAll('input'));
    let result = [];
    for (const checkedEl of checkbox) {
        let obj = {};
        if(checkedEl.checked === 'true'){

        }
    }

}